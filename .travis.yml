language: python
python:
    - "2.7"
install:
    - pip install requests
    - pip install coveralls
    - pip install pep8
    - python setup.py install
before_script:
    - pep8 modules/qs
script:
    - nosetests --with-coverage
after_success:
    - coveralls
